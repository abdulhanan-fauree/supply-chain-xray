# CognoDB capability report

Generated by `npm run probe`. CognoDB speaks openCypher over Bolt and works
with the official Neo4j driver, but it is a reimplementation rather than Neo4j —
it reports itself as `Neo4j/5.26.0` while surfacing Go type names in errors. Every
feature this application depends on is therefore verified against a live instance
before the data model or query layer relies on it.

| Capability | Result | Detail | Why it matters |
| --- | --- | --- | --- |
| Basic round trip | supported | returned hello/2 | Confirms Bolt handshake, TLS and auth all work. |
| Server identity | supported | Neo4j/5.26.0, Bolt 5.4 | Records the Bolt protocol version we are actually negotiating. |
| Uniqueness constraint | supported | created (IF NOT EXISTS accepted) | The seed script MERGEs packages and versions by key; without a unique constraint, concurrent batches create duplicates. |
| Secondary index | supported | created | Package name and vulnerability id lookups must not be full scans on a 0.5 vCPU instance. |
| Schema introspection | supported | SHOW CONSTRAINTS returned 1 row(s) | Lets the reset script drop exactly what the seed script created. |
| Batched write via UNWIND | supported | 500 rows: 500 nodes created, 1500 properties set | The whole seeding strategy is UNWIND over batches of rows; row-at-a-time writes would take hours. |
| Relationship properties | supported | 99 relationships with properties | DEPENDS_ON carries range/dev/optional, and traversals filter on those mid-walk. |
| Variable-length traversal | supported | reached 8 nodes within 8 hops | Blast radius is a *1..8 pattern. This is the single most important capability in the app. |
| Edge-property filter inside a traversal | supported | reached exactly 3 of 8 nodes, stopping at the flagged edge | Optional dependencies must be excluded partway through a walk, not after it, so 'what if optional deps are not installed' is answerable. |
| Bare list variable on a variable-length pattern | absent, as expected | none() requires list, got *types.Path | Expected to FAIL. This is a real divergence from Neo4j and the reason every traversal in this codebase names its path. |
| shortestPath() | supported | 1 hops: probe-0 -> probe-1 | Every 'how does A reach B' answer in the UI is a shortest path, rendered as a chain. |
| Multi-type variable-length pattern | supported | reached 5 nodes across two relationship types | Traversals start on :USES from an App and continue on :DEPENDS_ON, in one pattern. |
| CALL {} subquery | supported | 72 roots, 15 edges via subquery | Per-row aggregation (deps per app, vulns per package) is cleanest as a subquery. |
| List/map projection helpers | supported | collect+reduce ok (depthSum=9, 5 items) | Paths are projected to plain JSON for the UI using list comprehensions and collect(). |
| Query plan (EXPLAIN) | supported | plan root: ProduceResults | Needed to confirm traversals use indexes rather than scanning, given 256 MB of RAM. |
| APOC availability | absent, as expected | Unknown function 'apoc.version' | Expected to be absent. Confirming it lets us guarantee the app is pure openCypher. |

## The one divergence that shaped the code

In Neo4j, `MATCH (a)-[rels:TYPE*1..8]->(b)` binds `rels` to a **list of
relationships**, so `none(r IN rels WHERE r.optional)` is the natural way to filter
an edge property partway through a traversal. CognoDB binds that variable to a
**Path**, and list functions reject it:

```
none() requires list, got *types.Path
size() requires string or list, got *types.Path
```

The fix is to name the path and go through `relationships()`, which is verified
working above:

```cypher
MATCH p = (a)-[:DEPENDS_ON*1..8]->(b)
WHERE none(r IN relationships(p) WHERE r.optional)
```

Every traversal in this codebase names its path for that reason. `nodes(p)`,
`relationships(p)`, `length(p)`, `size()`, `all()`, `any()`, `none()` and `reduce()`
all behave normally once the path is named.
